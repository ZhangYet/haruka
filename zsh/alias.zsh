# Aliases
# ------------------------------

# Git aliases are provided by the oh-my-zsh git plugin loaded in plugin.zsh
# Common ones include: g, ga, gaa, gc, gco, gb, gl, gd, gp, gpl, etc.
# 
# Directory jumping function j() is defined in plugin.zsh after zsh-z loads
